# 🌊 心灵驿站 · 心理委员日常维护手册（详细版）

版本：Supabase 云端信箱版。原则：**先备份、再清理；涉隐私、不外传；定期归档、学期收尾。**

---

## 一、你需要盯的三处"数据存在地"

| 存在地 | 内容 | 谁可见 |
| --- | --- | --- |
| ① Supabase 云端（你的项目） | 广场留言、私聊、好友、图片视频 | 任何拿到网页（公开密钥）的人，本质=你们班的信任圈 |
| ② 各同学本机浏览器 | 各自的身份ID、好友、缓存记录 | 只有本机使用者自己 |
| ③ 你导出的备份文件（JSON/TXT） | 全部（含你的密令） | 只有你保管的人/地方 |

**日常维护 = 管好①的体量 + 常做③的备份 + 留意②的安全。**

---

## 二、日常：每次用后 3 分钟

1. **看一眼广场有没有需要关心的留言**
   - 深夜倾诉、情绪低落、提到"自杀/不想活"等（页面会自动弹"驿站小助手"提醒并给热线）。
   - 作为心理委员：私聊那位同学关心一下（可用你的匿名号私聊，说明你是心委）；
   - 涉及**自伤/伤他**：按保密例外，及时告知辅导员/家长并陪伴拨打 **12356 或 110**，不要独自处理，也不要答应"替他保密"。
2. **留意有没有骚扰/攻击/贴标签内容**：有的话私下提醒对方；情节严重可把对应记录删掉（网页上删除仅影响"本机"——云端删除要到 Supabase 控制台，见下方"清理"）。
3. 顺手确认自己设备右上角是绿色「在线」；若某天打不开，多半是网络，稍后重试即可。

---

## 三、每周一次（约 10 分钟）

### 1. 导出并归档备份（最重要）
打开你自己的 `index.html` →「⚙️ 我的」→「⬇️ 导出完整备份」，
把下载的 JSON 改名为 `心灵驿站备份_YYYY-MM-DD.json`，保存到：
- 你电脑上一个**加密/私密文件夹**（或压缩包加密）；
- 再复制一份到你自己私有的云盘（别用班级共享盘！备份里含你的密令，泄露=身份与私信可被读取）。

### 2. 看一眼用量
Supabase 控制台 → 你的项目 → 左侧 **Usage**（或 Billing → Usage）：
- 数据库大小：免费 500MB，文字量很小，基本无忧；
- Storage/文件：免费 1GB，**图片视频主要占这里**，重点盯；
- API 请求数：免费额度很大，8 秒轮询几十人没问题，若某周暴涨说明用的人很多或有人常开页面。

### 3. 清理一周前的图片/视频（可选但推荐）
因为网页只自动保留并拉取**每段对话最近 200 条**，更早的图片/视频若不删会一直占云空间：
- Supabase → 左侧 **Storage → media** 桶 → 按时间勾选 7 天前的文件 → **Delete**。
- 重要图片请先在本机另存或包含在周备份里（备份会带上记录与链接，但**媒体文件本体不进入 JSON**，需另存原文件）。

---

## 四、每月一次（约 15 分钟）

1. **再做一次"备份可用性"演练**：换一台设备/无痕窗口打开 `index.html` → 新匿名身份进入 →「我的 → 导入备份」→ 确认广场与私聊都回来了。这能保证真出事时你会恢复。
2. **决定文字记录去留**（二选一，先想好再执行）：
   - 策略 A【保留型，推荐做“心理委员”】：文字记录体积极小，保留一整个学期，学期末统一归档；
   - 策略 B【精简型】：每月导出备份后，删除 30 天前的文字。
   删除云端文字方法：Supabase → **Table Editor → `msgs`** → 按 `ts` 过滤（列最左筛选）勾选 → 删除行；或 SQL Editor 执行：
   ```sql
   delete from public.msgs
   where ts < extract(epoch from now() - interval '30 days') * 1000;
   ```
   ⚠️ 任何删除都请**先完成当月导出备份**再执行。
3. **核对好友与身份**：有没有已退班/不再参与的同学还在好友列表？双方任一方删除后云端即解除（「删除好友」按钮）。
4. **确认网页是最新版**：我后续若帮你加功能会更新 `index.html`，记得用新文件替换并重测一次收发。

---

## 五、学期末 / 每学期开学前（一次 30 分钟）

### 收尾（期末）
1. 做一次**最终全量导出**：`心灵驿站备份_期末_YYYY-MM-DD.json` → 本地加密 + 私有云盘双备份；
2. 把重要私聊内容（涉个别同学求助的）单独整理一份**脱敏备忘**（不出现可识别信息），供自己新学期跟进或按需告知辅导员；
3. 清理并"重置"环境可选：学期结束、无人使用时
   - 保留数据 → 项目放着即可（免费项目不活跃也不扣费；可在 Supabase 里 **Pause project** 暂停，随时恢复）；
   - 想全新开始 → 开学前在 SQL Editor 执行：
     ```sql
     delete from public.msgs; delete from public.friend_reqs; delete from public.friendships; delete from public.profiles;
     ```
     并到 Storage → media 清空文件；同学们各自「清空本机缓存」即可获得全新身份与空广场。
4. 写 3~5 行《本学期心理支持小结》给自己：常见话题、哪些同学被关心过（脱敏）、下学期可改进处。

### 开学（初始）
- 发布新版通知（若换了 index.html），提醒同学：
  - 换设备=新身份；要延续旧身份请导入上学期备份；
  - 老身份还在旧设备浏览器里，可直接继续用。

---

## 六、数据规模边界（务必知道）

| 项 | 限制 | 对策 |
| --- | --- | --- |
| 网页自动拉历史 | 广场/每段私聊只自动取**最近 200 条** | 更早记录靠**周/月备份**归档，换设备靠"导入备份"找回 |
| 数据库 | 免费 500MB（文字几乎用不完） | 无需处理 |
| 文件(图片视频) | 免费 1GB | 每周删 7 天前旧媒体 |
| 单文件 | 网页限 图片≤3MB / 视频≤8MB | 让同学发前压缩 |
| API/流量 | 免费额度对班级绰绰有余 | 偶发超时多为境外网络，重试即可 |

---

## 七、安全红线（复习）

1. **导出备份含你的密令**——备份文件只进你的私密存储，绝不上传班级群/公共盘。
2. `secret key / service_role` 永不外发、永不进网页。
3. 网页里的公开密钥 + URL 任何人拿到都能读写你 4 张表 → **本项目只放匿名倾诉内容**；不要出现真实姓名、学号、家庭住址等。
4. 保密例外要提前向同学说明（自伤他伤需上报）——这是专业规范，不是背叛信任。
5. 本平台替代不了专业帮助：热线 **12356**、青少年 **12355**、紧急 **110**；重要个案请联系学校心理咨询中心/辅导员。
