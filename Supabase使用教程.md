# 🌊 心灵驿站 · Supabase 使用教程（约 15 分钟，全程网页操作、无命令行）

本版只有 **一个 index.html + 一个 supabase.sql**。数据存在 **Supabase（免费额度，永久免费模式）**：
- 注册只需邮箱（免实名）；
- 免费额度：500MB 数据库、1GB 文件、每月大量 API 额度，班级规模用不完；
- 网页可在本机打开、也可发给同学各自打开，无需服务器与任何部署工具。

> ⚠️ 唯一注意事项：Supabase 服务器在境外（新加坡区），国内网络**偶尔慢或需重试**；
> 若发现连不上，多数情况稍等几秒自动恢复，也可点「重试连接」。

---

## 第 1 步：注册并创建项目（一次性）

1. 打开 **<https://supabase.com>** → Sign up（邮箱 + 密码 → 邮箱里点确认链接）。
2. 登录后点 **New project**：
   - Name：随意（如 `xlz`）
   - Database Password：设一个你记得住的密码（网页版不需要它，先记下即可）
   - Region：选 **Southeast Asia (Singapore)**（你已建好的 Mumbai 项目也能正常用，仅延迟略高）
   - Pricing：Free（免费档）
3. 等约 1～2 分钟，项目创建完成。

## 第 2 步：运行建表脚本（最关键一步）

1. 进入你的项目 → 左侧 **SQL Editor**（SQL 编辑器）；
2. 点 **New query**（新建查询）；
3. 把随附的 **supabase.sql** 的**全部内容**复制粘贴进去；
4. 点 **Run / 运行**。
运行成功会提示完成；之后左侧 **Table Editor** 应能看到 4 张表：`profiles`、`msgs`、`friend_reqs`、`friendships`；**Storage** 里有 `media` 桶。

## 第 3 步：复制两个密钥

左侧 **Settings → API**（或 Project Settings → API）：
- **Project URL**：形如 `https://xxxx.supabase.co`
- **anon public key**：以 `eyJhbGciOi...` 开头的一长串

## 第 4 步：打开网页并填入密钥

1. 双击打开 `index.html`（或发给同学各自打开）；
2. 选头像、起匿名昵称进入（**无需填密钥**：地址与公开密钥已内置，打开自动连接）；
3. 右上角变绿 **「在线」** 即成功；
4. **双设备验证**：两台手机/电脑各打开一次（可用无痕窗口当第二人），A 发一条留言 → 显示「已入云端」；B 打开页面 / 点「立即刷新」即可看到。

> 只有当你改连**另一个 Supabase 项目**时，才需要去「我的 → 云端」修改（会覆盖内置默认值）。

## 功能

- 🌊 匿名广场：文字/图片/视频留言，云端长期保存
- 👥 加好友（离线可收申请）+ 私聊留言 + 未读角标
- 🌈 心情日记、🧘 呼吸放松、📖 心理资源（本机私密）
- ⚙️ 我的：改头像昵称、重新匿名、导出完整备份(JSON/TXT)、断网自动补发
- 🔔 每 8 秒自动取信一次（非实时；无需双方同时在线）

## 清理 / 隐私 / 说明

- 想删旧数据：Supabase → Table Editor → 选中行删除；Storage → media 删除文件。
- 匿名：无账号无真名；但**消息以明文存储**，网页里带着 anon 密钥（可读可写你的 4 张表），任何拿到网页的人都能读到内容——适合**班级互信小圈子**，勿聊高度敏感信息；重要记录定期「导出完整备份」。
- 若某时段 Supabase 访问不稳定，属境外节点通病：稍等重试即可；完全不能接受时可换回 Bmob（¥30/月）或国内实名方案。
- 平台不能替代专业帮助：紧急请拨 **12356 / 110**。
